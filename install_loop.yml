---
- host: all
  become: yes
  gather_facts: no
  task:
  - name: install git, wget, tree on nodes
    apt:
     name: "{{ item }}"
     state: present
    with_items:
     - git 
     - wget
     - tree
     